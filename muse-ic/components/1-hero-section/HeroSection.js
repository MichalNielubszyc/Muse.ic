import {
  SectionContainer,
  SectionContent,
} from "../styled-components";
import { NavBar } from "./NavBar";

export const HeroSection = () => {
  return (
    <SectionContainer>
      <SectionContent>
          <NavBar />
      </SectionContent>
    </SectionContainer>
  );
};
